kind: ResourceQueryAdapter
id: nectar.adapters.resources-query-adapter
category_resources:
  compute:
    - deployments
    - jobs
  network:
    - services
    - ingress
  storage:
    - persistentvolumeclaims
  config:
    - configmaps
    - secrets
    - resourcequotas
  policy:
    - roles
    - rolebindings
    - serviceaccounts